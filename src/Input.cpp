#include "Input.h"

#include "SDL3/SDL_scancode.h"

void ProcessInput(const bool& keyStates[])
{
    if (keyStates[SDL_SCANCODE_W])
    if (keyStates[SDL_SCANCODE_A])
    if (keyStates[SDL_SCANCODE_S])
    if (keyStates[SDL_SCANCODE_D])
}
